/**
 * App — Producto principal (dashboard, proyectos, FinOps).
 *
 * Este paquete será bootstrappeado como una app Next.js (App Router) en la
 * Phase 2. Por ahora es un marcador de paquete válido dentro del monorepo Nx.
 *
 * @packageDocumentation
 */

export const appConfig = {
  name: "app",
  description: "SaaS PM+FinOps — Producto principal (dashboard, proyectos, FinOps)",
  version: "0.1.0",
} as const;
