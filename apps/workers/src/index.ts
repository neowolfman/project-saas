/**
 * Workers — FastStream message consumers (event-driven).
 *
 * This barrel is the entry point for the FastStream application.
 * The actual FastStream app is defined in `runner.py` and
 * launched via `faststream run apps.workers.runner:app`.
 *
 * @packageDocumentation
 */

export {};
