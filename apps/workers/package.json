{
  "name": "@saas/workers",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS PM+FinOps Workers (FastStream) — marcador de paquete Nx. Las dependencias Python viven en pyproject.toml.",
  "scripts": {
    "dev": "echo 'Usar: faststream run apps.workers.src.runner:app --reload (desde venv Python)' && exit 1",
    "start": "echo 'Usar: faststream run apps.workers.src.runner:app (desde venv Python)' && exit 1"
  }
}
