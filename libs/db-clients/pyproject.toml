[project]
name = "saas-db-clients"
version = "0.1.0"
description = "DB Connection routing and RLS session factory for SaaS PM+FinOps"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.35",
    "asyncpg>=0.29.0"
]
